name: diviner

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: diviner
      POSTGRES_PASSWORD: sekret
    ports:
      - 5432:5432
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
